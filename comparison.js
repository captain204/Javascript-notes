console.log(3 < 2 < 1);

//Strict Comparison

var a = 0;

var b = false;

// Comparisons should be done with the strict comparison operator

// Double equals should only be used when you want to coerce the two values

if(a === b)
{
    console.log("They are equal");
}else{

    console.log("They are not equal");
}